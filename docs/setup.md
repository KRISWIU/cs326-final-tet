If node.js is not installed, it will need to be installed first.

The following modules will need to be installed:

- Express
- MongoDB

To install these, use `npm install express` and `npm install mongodb` respectively.